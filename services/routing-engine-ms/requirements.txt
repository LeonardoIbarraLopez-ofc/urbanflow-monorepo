# requirements.txt — Dependencias Python de routing-engine-ms.
# Motor de rutas multimodales: FastAPI + Neo4j + servidor ASGI Uvicorn.

fastapi==0.111.0
uvicorn[standard]==0.29.0
neo4j==5.20.0
pydantic==2.7.1
httpx==0.27.0      # Cliente HTTP para llamadas a otros microservicios
pytest==8.2.0
ruff==0.4.4        # Linter y formateador
